# Only needed in production, where the backend is a separate host (e.g. Render).
# Leave unset for local dev — Vite's proxy handles /api and /socket.io instead.
VITE_API_URL=https://your-backend.onrender.com
